{
  "name": "@waptik/graphql-pusher-subscriptions",
  "version": "0.1.6",
  "description": "GraphQL subscriptions with Pusher",
  "author": "@waptik @alefemafra @sachinp-git",
  "license": "MIT",
  "main": "./lib/index.js",
  "scripts": {
    "test": "echo \"Who need tests anyway? ¯\\_(ツ)_/¯ \" && exit 0",
    "build": "tsc -p .",
    "incver": "npm version patch -m \":package: release v%s\"",
    "push": "npm run incver && git push",
    "bump": "npm run incver && git push --follow-tags"
  },
  "dependencies": {
    "graphql-subscriptions": "^1.1.0",
    "iterall": "^1.2.2",
    "pusher": "^3.0.1",
    "pusher-js": "^6.0.3"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  }
}
